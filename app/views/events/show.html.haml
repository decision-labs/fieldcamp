/ = render :partial => "shared/nav"
/ TODO: refactor - currently used in the info-bubble
#box.content
  #block-login.block
    %h2=t('activity_details')
    .content
      =render :partial => "event", :locals => {:event => @event, :project => @event.project}
