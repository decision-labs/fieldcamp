= render :partial => "nav"

.block
  .content
    = model_title_links(@location, t="name")
    .inner
      %p= raw(RDiscount.new(@location.description).to_html) rescue nil


=render :partial => 'projects/project', :collection => @location.projects

= link_to raw("&larr; #{t(:back_to_locations)}"), locations_path

