- content_for :head do
  = stylesheet_link_tag 'map'
  = javascript_include_tag 'vendor/GeoJSON', 'map-helpers'
  = render 'js/show-map_js'

#map
  #map_canvas

.project
  =project_title_links(@project)
  .project-location= @project.location.name
  .project-description= @project.description
  .project-time
    Start date:
    = @project.start_date
  .project-time
    End date:
    = @project.end_date
  .project-partners
    Partners:
    = render :partial => 'partner', :collection => @project.partners
  .project-sectors
    Sectors:
    = render :partial => 'sector', :collection => @project.sectors
  .clear

  %p
    %span.new-event-link=link_to t(:new_event), new_project_event_path(@project)

  %h2
    = t(:recent_events)
  = render :partial => 'events/event', :collection => @project.events

= link_to raw("&larr; #{t(:back_to_projects)}"), projects_path
